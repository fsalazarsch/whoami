<%= form_with(model: contact) do |form| %>
  <% if contact.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
        <% contact.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :dataName, style: "display: block" %>
    <%= form.text_field :dataName %>
  </div>

  <div>
    <%= form.label :dataValue, style: "display: block" %>
    <%= form.text_area :dataValue %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
