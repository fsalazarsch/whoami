<%= form_with(model: addon) do |form| %>
  <% if addon.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(addon.errors.count, "error") %> prohibited this addon from being saved:</h2>

      <ul>
        <% addon.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :category, style: "display: block" %>
    <%= form.text_field :category %>
  </div>

  <div>
    <%= form.label :key_value, style: "display: block" %>
    <%= form.text_field :key_value %>
  </div>

  <div>
    <%= form.label :value, style: "display: block" %>
    <%= form.text_field :value %>
  </div>

  <div>
    <%= form.label :percent, style: "display: block" %>
    <%= form.number_field :percent %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
