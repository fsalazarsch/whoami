<%= form_with(model: networking) do |form| %>
  <% if networking.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(networking.errors.count, "error") %> prohibited this networking from being saved:</h2>

      <ul>
        <% networking.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :profile, style: "display: block" %>
    <%= form.text_field :profile %>
  </div>

  <div>
    <%= form.label :url, style: "display: block" %>
    <%= form.text_field :url %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
